# Nome da aplica��o
spring.application.name=Attendance Tracker

# Configura��es do banco de dados H2 para ambiente de desenvolvimento e testes
spring.datasource.url=jdbc:h2:mem:cadastro
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=org.h2.Driver

# Configura��es do Hibernate para o H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Configura��o de seguran�a
spring.security.user.name=admin
spring.security.user.password=admin

# Configura��o do servidor web
server.port=8083

# Configura��o de logging
logging.level.root=INFO
